/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Static export for GitHub Pages
  output: 'export',
  // Project Pages base path (https://<user>.github.io/ICO910)
  basePath: '/ICO910'
}

module.exports = nextConfig
